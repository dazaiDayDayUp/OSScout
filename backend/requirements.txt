# 核心框架
fastapi==0.115.*
uvicorn[standard]
pydantic==2.10.*
pydantic-settings

# HTTP 客户端
httpx>=0.27.0

# 数据库 & 缓存
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
alembic>=1.14.0
redis>=5.0.0

# 工具
python-dotenv
structlog
pytest
pytest-asyncio

# === 以下为后续 Phase 需要的依赖，按需启用 ===

# Phase 1.3: 代码分析
semgrep>=1.100.0
radon>=6.0.0
# pygithub>=2.5.0

# Phase 1.2: MCP 协议
mcp>=1.0.0

# Phase 2: 任务队列
celery[redis]>=5.4.0

# Phase 3: LLM + RAG
openai>=1.60.0
chromadb>=0.6.0
sentence-transformers>=3.0.0
